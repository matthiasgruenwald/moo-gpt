# P10 — Repository-Veröffentlichung (letztes Paket)

**Voraussetzung:** Alle anderen Pakete ✓

**`/security-review` + manuell vor Commit.**

- API-Keys prüfen
- `.env.example` erstellen
- `README.md` neu schreiben
- `CHANGELOG.md` anlegen
- `LICENSE` hinzufügen
- Repo public schalten

## Verification

`npm install && node server.js` auf frischer Instanz
