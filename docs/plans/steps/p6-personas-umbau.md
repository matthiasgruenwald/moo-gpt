# P6 — Personas-Umbau (1 Issue, groß) ✓ done

**Voraussetzung:** P5 ✓

Neue `personas`-Tabelle mit `teacher_id` (NULL = global). 10 globale Seed-Personas via `seeds/personas.js`. Zwei API-Ebenen (Lehrer + Admin). Dashboard: Pill-Anzeige globale Typen, editierbare eigene Personas, Admin-View mit Lehrername-Filter.
