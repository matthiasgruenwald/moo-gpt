# Handoff: moo-gpt Session 2026-05-23 (Abend)

## Projekt

moo-gpt — KI-Chatbot-Widget für Moodle. Repo: `matthiasgruenwald/moo-gpt`, Pfad auf LXC: `/opt/moo-gpt`, Dienst: `systemctl restart moo-gpt`.

Stack: Node.js 22, Express, express-ws, OpenAI Responses API (MODEL_NAME = gpt-5, GEN_MODEL = gpt-4.1-nano), SQLite (better-sqlite3 synchron), Vanilla JS Widget.

---

## Was diese Session erledigt hat

### Verifikation #41 / #42 / #43 — alle bereits implementiert

Der vorherige Handoff hatte diese drei Issues als „offen" markiert. Verifiziert:

| Issue | Status | Commit |
|-------|--------|--------|
| #41 Kosten-Bug | ✅ `token_log.model` pro Eintrag, `getThreadCostByModel` gruppiert nach Modell | `dc21bf7` |
| #42 Widget Links/Rechts-Toggle | ✅ `_toggleSide()`, sessionStorage, `left-side`-CSS-Klasse | `8d6f5df` |
| #43 Stop-Button im Widget | ✅ `stop-icon`, Lock-Modal, `_submitLock()` / `_submitUnlock()` | `8d6f5df` |

Ebenso bestätigt: #48 (`mmbbs-bot.js löschen`) ist in `18ea6e2` erledigt.

### GitHub-Issues: Massenabschluss ausstehend

Es wurden **keine neuen Commits** in dieser Session erstellt. Die Hauptaufgabe war Diagnose und Lagebestimmung.

---

## Roadmap-Stand

```
Phase 1  | #41 Kosten-Bug          ✅ implementiert, GitHub noch offen
         | #48 mmbbs-Reste         ✅ implementiert, GitHub noch offen
Phase 2a | #42 Widget Links/Rechts ✅ implementiert, GitHub noch offen
Phase 2b | #43 Stop-Button Widget  ✅ implementiert, GitHub noch offen
Phase 2c | #58 Dashboard-Umbau     ✅ implementiert, GitHub noch offen
Phase 2d | #52 Live-Überblick      ✅ implementiert, GitHub noch offen
Phase 2e | #51 Inline-Edit         ✅ implementiert, GitHub noch offen
Phase 3  | #46/#53/#54 Memory      ✅ implementiert, GitHub noch offen
Phase 4  | #47/#55/#56 Assistent   ✅ implementiert, GitHub noch offen
         | #44 Dashboard Routing   ✅ implementiert, GitHub noch offen
         | #45 Nachrichten-Edit BE ✅ implementiert, GitHub noch offen
         | #49 Navigation-Bar      ✅ implementiert, GitHub noch offen
         | #50 Dashboard-Redirect  ✅ implementiert, GitHub noch offen
         | #57 Prompt-Verlauf fix  ✅ implementiert, GitHub noch offen
```

**Wirklich noch offen (nicht implementiert):**

| # | Titel | Prio |
|---|-------|------|
| #6 | Chat-Verwaltung: Neue Chats, Archivierung & sicheres Löschen | mittel |
| #7 | Rollenerkennung für tegpt (iframe) | mittel |
| #9 | Snippet für Lehrer-Übersicht über mehrere Aktivitäten | niedrig |
| #22 | Lehrkraft-eigene Bot-Avatare per Dashboard | niedrig |
| #23 | Audio-Eingabe via Whisper | niedrig |
| #24 | Audio-Ausgabe via TTS | niedrig |
| #32 | Azure OpenAI / EU-Inferenz-Residency | niedrig |

---

## Nächste Schritte (empfohlene Reihenfolge)

### 1. Alle erledigten Issues in GitHub schließen

```bash
# Phase 1
gh issue close 41 --repo matthiasgruenwald/moo-gpt \
  --comment "Fix in dc21bf7: token_log.model pro Eintrag, getThreadCostByModel gruppiert nach Modell, sumCostRows berechnet Kosten per Modell."
gh issue close 48 --repo matthiasgruenwald/moo-gpt \
  --comment "Erledigt in 18ea6e2: mmbbs-bot.js gelöscht, keine Referenzen mehr im aktiven Code."

# Phase 2
gh issue close 42 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in 8d6f5df: _toggleSide() mit sessionStorage, Toggle-Button im Header."
gh issue close 43 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in 8d6f5df: stop-icon nur für Lehrkräfte, Lock-Modal, _submitLock()/_submitUnlock()."
gh issue close 44 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in Wave 1 (8d6f5df): Dashboard Routing-Shell."
gh issue close 45 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in Wave 2: Nachrichten-Edit Backend (DB-Migration + PUT /api/messages/:id)."
gh issue close 46 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in Wave 2: Schüler-Memory Backend + Widget-Feedback-Button."
gh issue close 47 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in Wave 2: Prompt-Assistent Direktmodus."
gh issue close 49 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in Wave 2 (ea16261): Dashboard Navigation-Bar."
gh issue close 50 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in Wave 2 (ea16261): Dashboard-Redirect mit localStorage last-page."
gh issue close 51 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in Wave 2 + f564388: Nachrichten-Edit Dashboard-UI."
gh issue close 52 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in Wave 2 + Session 2026-05-23: Live-Unterrichts-Überblick."
gh issue close 53 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in Wave 2 (ea16261): Widget Memory-Overlay."
gh issue close 54 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in Wave 2 (ea16261): Teacher Memory-Management im Dashboard."
gh issue close 55 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert in Wave 2 (ea16261): Prompt-Assistent Rückfragen-Modus + DB-Präferenz."
gh issue close 56 --repo matthiasgruenwald/moo-gpt \
  --comment "Implementiert: Suggest-Interview analysiert Lücken (9480622)."
gh issue close 57 --repo matthiasgruenwald/moo-gpt \
  --comment "Gefixt in f564388: Prompt-Verlauf max-height entfernt."
gh issue close 58 --repo matthiasgruenwald/moo-gpt \
  --comment "Erledigt: chats/settings/overview als separate Seiten, Widget-Link auf /dashboard gefixt."
```

### 2. Nächstes Feature wählen

Empfehlung: **#6 Chat-Verwaltung** (Neue Chats, Archivierung, Löschen) — das ist das einzige offene Issue mit echter Schüler-/Lehrer-Relevanz im Alltag.

Alternativ: **#7 Rollenerkennung tegpt** wenn der iframe-Kontext wichtiger ist.

---

## Nicht-offensichtliche Details

- **`computeRunCost` (sync-Export in `token-log.js` Zeile 98):** Wird nirgendwo aufgerufen — toter Export. Kann bei Gelegenheit entfernt werden.
- **`getMessagesAll` in `stores/chat.js`:** Liefert absichtlich kein `cost_model` (nur für KI-History-Building, nicht für Kostenanzeige).
- **`public/settings.js`:** Eigenständig ohne Abhängigkeit zu `dashboard.js`. Utility-Funktionen (`escHtml`, `formatTime`, `parseUTC`, `apiFetch`) sind dupliziert — Absicht, kein Shared-Module-Overhead.
- **`message_count` in `stores/dashboard.js`:** Zählt nur `role='user' AND content_type != 'task_image'`. Opener-Bild zählt nicht als Chat.
