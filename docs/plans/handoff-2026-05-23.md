# Handoff: moo-gpt Session 2026-05-23

## Projekt

moo-gpt — KI-Chatbot-Widget für Moodle. Repo: `matthiasgruenwald/moo-gpt`, Pfad auf LXC: `/opt/moo-gpt`, Dienst: `systemctl restart moo-gpt`.

Stack: Node.js 22, Express, express-ws, OpenAI Responses API (MODEL_NAME = gpt-5, GEN_MODEL = gpt-4.1-nano), SQLite (better-sqlite3 synchron), Vanilla JS Widget.

Entwicklung direkt auf dem LXC. Kein Deploy-Snippet nötig außer wenn explizit gefragt.

---

## Was diese Session erledigt hat

### Wave 2 implementiert + committed (`ea16261`)

| Issue | Titel | Status |
|-------|-------|--------|
| #49 | Dashboard Nav-Bar | ✅ committed — aber mit Bug (siehe #58) |
| #50 | Dashboard localStorage Redirect | ✅ committed |
| #51 | Nachrichten-Edit Inline-UI + Versionshistorie | ✅ committed + activityId-Fix |
| #52 | Live-Unterrichts-Überblick (/dashboard/overview) | ✅ committed |
| #53 | Widget Memory-Overlay für Schüler (🧠) | ✅ committed |
| #54 | Teacher Memory-Management im Dashboard | ✅ committed |
| #55 | Prompt-Assistent Rückfragen-Checkbox + DB-Präferenz | ✅ committed |

### Prompt-Assistent (#47/#56) vollständig überarbeitet

Letzter Stand: `8cf2ccd` — der Assistent:
- Analysiert vorhandenen Prompt + Moodle-Aufgabe als Kontext
- Fragt nur nach echten Lücken (kein starres Fragenlimit)
- Gibt zu jeder Frage eine eigene Empfehlung mit (grill-me-Stil)
- Nutzt gpt-5 (MODEL_NAME)
- Ergebnis als Gegenüberstellung (alter vs. neuer Prompt) — identisch zum Prompt-Check-Flow
- Issue #56 geschlossen ✅

### Fixes in dieser Session

- **Inline-Edit "Forbidden"** (`f564388`): `activityId` fehlte in allen 4 message-edit API-Calls → ergänzt
- **Suggest-Panel z-index mobile** (`f564388`): z-index 10001 auf mobile, liegt jetzt über Config-Overlay
- **Prompt-Verlauf Abschneidung** (#57, `f564388`): `max-height: 600px` durch `display: none/block` ersetzt

---

## Offener Stand: Issue #58 — Dashboard-Struktur

### Problem

Agent #49 hat `#dashboard-nav` (Seitenlinks: Chats | Überblick | Einstellungen) korrekt hinzugefügt, aber den alten `#tab-bar` (In-Page: Schüler | Optimierung | Einstellungen | Info | Admin) nicht entfernt. Ergebnis: zwei Tab-Leisten übereinander, Dashboard sieht aus wie vorher.

### Gewünschter Zielzustand

| Seite | Inhalt |
|-------|--------|
| `/dashboard/chats` | Schüler-Chats + Optimierung |
| `/dashboard/overview` | Live-Überblick ✅ bereits fertig |
| `/dashboard/settings` | Einstellungen + Admin + Info (aus chats.html auslagern) |

### Betroffene Dateien

- `views/dashboard/chats.html` (~1560 Zeilen) — `#settings-panel`, `#admin-panel`, `#info-panel` auslagern; `#tab-bar` auf Schüler+Optimierung reduzieren
- `views/dashboard/settings.html` (aktuell Stub, 92 Zeilen) — Inhalt aus chats.html übernehmen
- `public/dashboard.js` — prüfen welche Sektionen nur auf settings-Seite gebraucht werden

### Wichtige nicht-offensichtliche Details

- `views/dashboard/chats.html` lädt `public/dashboard.js` — das JS initialisiert alles was es im DOM findet
- `#settings-panel` enthält per-Aktivität-Config (Bot-Titel, Upload-Modus, Erfahrungsprompt)
- `#admin-panel` enthält globalen System-Prompt + Modell-Einstellung (nur für Admins sichtbar)
- `#info-panel` enthält Bedienungshinweise
- Der `#dashboard-nav` auf chats.html setzt Query-Params (activityId + token) per JS in die Links — das ist korrekt und soll bleiben

---

## Nächste Schritte (empfohlene Reihenfolge)

1. **Issue #58 angehen** (Dashboard-Umbau): chats.html verschlanken, settings.html befüllen
2. **Wave 3 starten** sobald #58 erledigt ist

### Noch offene GitHub-Issues (Wave 1+2 noch nicht geschlossen)

Issues #41–#55 wurden implementiert und committed aber nicht in GitHub geschlossen. Die nächste Session kann das nachholen oder ignorieren.

### Wave 3 Issues (noch nicht erstellt)

Aus `docs/plans/roadmap-2026.md` sind noch keine Issues für Phase 3+ angelegt.

---

## Empfohlene Skills für die nächste Session

- Kein spezieller Skill nötig — direkter Umbau von chats.html/settings.html
- `/karpathy-guidelines` aktivieren: chirurgische Änderungen, kein Overengineering
- Für den Umbau: Agent mit `isolation: worktree` starten (chats.html ist groß, Fehler sollten isoliert sein)
