config.json
node_modules
public/storage

config/server.cert
config/server.key
public/chat-icon.png
.gitlab-ci.yml

# SQLite-Datenbank (wird zur Laufzeit erzeugt)
*.db
*.db-shm
*.db-wal

# Lokale Overrides (nur lokal, nie committen)
CLAUDE.local.md
.claude/local/
.claude/settings.local.json
.claude/worktrees/

# macOS
.DS_Store

# Claude Code / ECC harness
.agents/
skills-lock.json
AGENTS.md
.claude/skills/

# graphify output (lebt auf graphify-data Branch)
graphify-out/
